onclick事件,函数参数，全局变量！
parseFloat(string);解析一个字符串->返回一个浮点数;
toFixed()把数字转化为字符串;四舍五入为指定小数位数的数字
removeClass()、addClass();被选元素移除或增加多个类
.val()返回或设置被选元素的value值;
	用于返回时，返回第一个匹配元素的value属性的值。
	用于设置时，设置所有匹配元素的value值。
.attr()设置或返回被选元素的属性和值
	用于返回时，返回第一个匹配元素的值
	用于设置时，为匹配元素设置一个或多个属性/值对
JSON.Parse()将一个JSON字符串转化为对象